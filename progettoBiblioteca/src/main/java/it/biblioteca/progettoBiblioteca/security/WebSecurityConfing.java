package it.biblioteca.progettoBiblioteca.security;
/*
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
import org.springframework.security.core.userdetails.User;
import org.springframework.security.core.userdetails.UserDetailsService;
import org.springframework.security.provisioning.InMemoryUserDetailsManager;

@Configuration
@EnableWebSecurity
public class WebSecurityConfing {
    @Bean
    public UserDetailsService userDetailsService() {
        InMemoryUserDetailsManager menager = new InMemoryUserDetailsManager();
        menager.createUser(User.withDefaultPasswordEncoder().username("admin").password("biblioteca").build());
        return menager;
    }
}

 */
